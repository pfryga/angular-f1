'use strict';

/**
 * @ngdoc filter
 * @name raceApp.filter:filterName
 * @function
 * @description
 * # filterName
 * Filter in the raceApp.
 */
angular.module('raceApp')
  .filter('filterName', function () {
    // return function (input, driver) {
    //   	var keyword = new RegExp(input, 'i');
    // 	return !input || keyword.test(driver.Driver.givenName) || keyword.test(driver.Driver.familyName);
    // };
  });
